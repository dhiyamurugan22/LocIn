package com.learning.coursemodule.model;

/**
 * The three teaching depths a learner can choose, independently, for every module.
 * Selecting a level never changes WHICH content a student can access - only how
 * it is explained and how deep the explanation goes.
 */
public enum TeachingLevel {
    EASY,
    MEDIUM,
    HARD
}
